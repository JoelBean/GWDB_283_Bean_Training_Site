<?php
includ_once("helpers.?>
returnjson<?php")
}

function returnJSON($returnData) {

  return json_encode($returnData);

}

// first we need to validate the incoming data a little more,

// then if it's valid we need to query the db and make sure the passwords match,
 ?>
